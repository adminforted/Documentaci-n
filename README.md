# Documentación
Para desplegar el backend es necesario instalar las dependencias en el nuevo entorno
pip install –r . \requirements.txt  # for windows
pip3 install –r . \requirements.txt  # for linux

si se actualizan las dependencias
pip freeze > requirements.txt   # for windows
pip3 freeze > requirements.txt  # for linux

